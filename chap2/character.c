#include <stdio.h>

int main (int argc, char *argv[]) {
    int one_alphabet, one_number;

    one_alphabet = 65;
    one_number = 48;

    printf("one_alphaber is %c\n", one_alphabet);
    printf("one_number is %c\n", one_number);
}